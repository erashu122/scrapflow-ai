# 12. Deployment

Use a CDN for the SPA, managed MongoDB Atlas with private networking, object storage with malware scanning, Redis for rate limiting/cache, and a managed container platform for API replicas. Run migrations/index verification at deploy time, expose health probes, centralise logs and metrics, and retain audit logs in immutable storage.
